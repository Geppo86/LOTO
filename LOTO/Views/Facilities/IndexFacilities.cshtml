@model IEnumerable<LOTO.Models.Facility>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
    @*<nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
            <li class="breadcrumb-item"><a href="/Action1?id=10">Action 1</a></li>
            <li class="breadcrumb-item active">Action 2</li>
        </ol>
    </nav>*@
<h1>Facilities</h1>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FacilityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FacilityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FID">Delete</a> |
                <button type="button" class="btn btn-link" onclick="location.href='@Url.Action("IndexBuildings", "Buildings")'">Buildings</button>
            </td>
        </tr>
}
    </tbody>
</table>
